# chunks

从 `01_sources/normalized/chapters/` 确定性切片（约 800–1200 字，重叠约 80）。  
港铃原作三章均短于窗口，故各一文件：`c001.md`–`c003.md`。

```bash
palimpsest context harbor-bell --write-chunks
```

L1 检索材料，不当系统指令。见 `docs/05-context.md`。
